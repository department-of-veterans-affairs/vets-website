//@Library('va.gov-devops-jenkins-lib') _

pipeline {
  agent {
    dockerfile {
      filename 'Dockerfile'
      //args "-v ${pwd()}:/application"
      label 'vetsgov-general-purpose'
    }
  }

  stages {
    stage('Visual Test') {
      steps {
        sh 'npm run test:vrt'
      }
    }
  }
}