// import { expect } from 'chai';
// import Calculator from '../../../src/js/gi/utils/Calculator.jsx';

describe('Calculator unit tests', () => {
  describe('Stanford University', () => {
    // const school = {};
    // school.bah = '1476';
    // school.country = 'USA';
    // school.institutionType = { name: 'ojt' };

    context('default dropdown selections', () => {
      // const calculator = new Calculator();
      // estimator.setMilitaryStatus = 'veteran';
      // estimator.setSpouseActiveDuty = 'no';
      // estimator.setGiBillChap = '33';
      // estimator.setNumberOfDepend = '0';
      // estimator.setPost911Elig = 'no';
      // estimator.setCumulativeService = '1.0';
      // estimator.setEnlistmentService = '3';
      // estimator.setConsecutiveService = '0.8';
      // estimator.setOnline = 'no';
      // estimator.setInstitutionType = school.institutionType.name;
      // estimator.setCountry = school.country;
      // estimator.setBah = school.bah;

      it('should return the correct tuition estimate', () => {
        // calculator.renderTuitionFees();
        // expect(calculator.results.tuition.qualifier).to.eql(null);
        // expect(calculator.results.tuition.value).to.eql('N/A');
      });

      it('should return the correct housing estimate', () => {
        // calculator.renderHousingAllowance();
        // expect(calculator.results.housing.qualifier).to.eql('per month');
        // expect(calculator.results.housing.value).to.eql(1476);
      });

      it('should return the correct books estimate', () => {
        // estimator.renderBookStipend();
        // expect(calculator.results.books.qualifier).to.eql('per year');
        // expect(calculator.results.books.value).to.eql(1000);
      });
    });
  });
});
