import { expect } from 'chai';

import { SET_DATA,
  SET_VALID,
  SET_EDIT_MODE,
  SET_PRIVACY_AGREEMENT,
  SET_SUBMISSION,
  SET_SUBMITTED
} from '../../../../src/js/common/schemaform/actions';

import createSchemaFormReducer from '../../../../src/js/common/schemaform/reducers';

describe('schemaform reducer', () => {
  it('', () => {
    
  });
});
