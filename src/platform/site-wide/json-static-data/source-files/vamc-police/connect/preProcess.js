/**
 * TODO: First need to know structure of police data/keys and whether it was processed into System or Facility
 *
 */
export const preProcessPoliceData = (collection = {}, _facilityEntity = []) => {
  // This never actually does anything in the EHR file. The return at the end is collection.

  return collection;
};
