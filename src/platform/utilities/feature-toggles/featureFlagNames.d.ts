// Type definitions for featureFlagNames module

/**
 * Feature flag names mapping
 * Keys are camelCase names, values are the actual feature flag names used in the API
 */
declare const featureFlagNames: Record<string, string>;

export default featureFlagNames;
