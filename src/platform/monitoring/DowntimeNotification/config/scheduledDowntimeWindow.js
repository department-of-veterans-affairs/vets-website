// import moment from 'moment-timezone';

// Global downtime notification will be set if status api fails and
// current time is during this window.
// example value: moment.tz('2020-01-12 00:01', 'America/New_York')

export default {
  downtimeStart: null,
  downtimeEnd: null,
};
