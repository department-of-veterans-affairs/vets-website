import React from 'react';

const FieldTemplateContext = React.createContext(null);

const FieldTemplateProvider = FieldTemplateContext.Provider;

export { FieldTemplateContext, FieldTemplateProvider };
