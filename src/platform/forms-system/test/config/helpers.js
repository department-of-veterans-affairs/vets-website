export const uploadStore = {
  getState: () => ({
    featureToggles: {
      // eslint-disable-next-line camelcase
      file_upload_short_workflow_enabled: true,
    },
  }),
  subscribe: () => {},
  dispatch: () => {},
};
