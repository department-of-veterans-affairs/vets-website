import React from 'react';

/**
 * This is a test field that will return a nice message
 *
 * @param none
 * @returns String that is friendly and nice
 */
const TestField = (): JSX.Element => {
  return <h1>Hello There</h1>;
};

export default TestField;
