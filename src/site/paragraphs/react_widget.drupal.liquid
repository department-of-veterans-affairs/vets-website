{% comment %}
  Example data:
  {
    block: {
      entity: {
        entityBundle: "react_widget"
        entityRendered: "  <div class="paragraph paragraph--type--react-widget paragraph--view-mode--full"></div>"
        entityType: "paragraph"
      }
    }
  }
{% endcomment %}

{{ block.entity.entityRendered }}
