{% if fieldRelatedInformation != empty and fieldRelatedInformation.0.entity.fieldLink %}
  <section class="vads-u-padding-top--3 vads-u-display--flex vads-u-flex-direction--column"
    data-template="includes/benefit-hubs-links">
    <!-- Section title -->
    <h2 class="vads-u-margin-top--0 vads-u-font-size--h3">Related information</h2>

    <!-- Links -->
    <ul class="usa-unstyled-list">
      {% for fieldRelatedInformationLink in fieldRelatedInformation %}
      <li>
        <p class="vads-u-margin--0">
          <strong>
            <a href="{{ fieldRelatedInformationLink.entity.fieldLink.url.path }}">
              {{ fieldRelatedInformationLink.entity.fieldLink.title }}
            </a>
          </strong>
        </p>
        <p>{{ fieldRelatedInformationLink.entity.fieldLinkSummary }}</p>
      </li>
      {% endfor %}
    </ul>
  </section>
{% endif %}
