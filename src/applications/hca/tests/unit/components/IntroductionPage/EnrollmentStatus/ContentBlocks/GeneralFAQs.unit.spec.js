import React from 'react';
import { render } from '@testing-library/react';
import { expect } from 'chai';

import GeneralFAQs from '../../../../../../components/IntroductionPage/EnrollmentStatus/ContentBlocks/GeneralFAQs';

describe('hca <faqBlock1>', () => {
  const { faqBlock1 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock1 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock2>', () => {
  const { faqBlock2 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock2 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock3>', () => {
  const { faqBlock3 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock3 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock4>', () => {
  const { faqBlock4 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock4 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock5>', () => {
  const { faqBlock5 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock5 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock6>', () => {
  const { faqBlock6 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock6 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock7>', () => {
  const { faqBlock7 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock7 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock8>', () => {
  const { faqBlock8 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock8 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock9>', () => {
  const { faqBlock9 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock9 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock10>', () => {
  const { faqBlock10 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock10 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <faqBlock11>', () => {
  const { faqBlock11 } = GeneralFAQs;
  it('should render', () => {
    const { container } = render(<faqBlock11 />);
    expect(container).to.not.be.empty;
  });
});
