import React from 'react';
import { render } from '@testing-library/react';
import { expect } from 'chai';

import WarningExplainations from '../../../../../../components/IntroductionPage/EnrollmentStatus/ContentBlocks/WarningExplainations';

describe('hca <explainBlock1>', () => {
  const { explainBlock1 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock1 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock2>', () => {
  const { explainBlock2 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock2 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock3>', () => {
  const { explainBlock3 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock3 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock4>', () => {
  const { explainBlock4 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock4 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock5>', () => {
  const { explainBlock5 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock5 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock6>', () => {
  const { explainBlock6 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock6 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock7>', () => {
  const { explainBlock7 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock7 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock8>', () => {
  const { explainBlock8 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock8 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock9>', () => {
  const { explainBlock9 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock9 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock10>', () => {
  const { explainBlock10 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock10 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock11>', () => {
  const { explainBlock11 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock11 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock12>', () => {
  const { explainBlock12 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock12 />);
    expect(container).to.not.be.empty;
  });
});

describe('hca <explainBlock13>', () => {
  const { explainBlock13 } = WarningExplainations;
  it('should render', () => {
    const { container } = render(<explainBlock13 />);
    expect(container).to.not.be.empty;
  });
});
