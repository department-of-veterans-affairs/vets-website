export function selectProfileLogInProvider({ profile }?: user.profile): strings;
export function selectProfileLoa({ profile }?: user.profile): number;
export function selectVaPatient({ profile }?: user.profile): strings;
