export default NonPatientLandingPage;
declare function NonPatientLandingPage({ data, recordEvent }: {
    data?: {};
    recordEvent?: any;
}): React.JSX.Element;
declare namespace NonPatientLandingPage {
    namespace propTypes {
        let data: PropTypes.Requireable<object>;
        let recordEvent: PropTypes.Requireable<(...args: any[]) => any>;
    }
}
import React from 'react';
import PropTypes from 'prop-types';
