export const queryCallButton = view => view.queryByTitle('Call');

export const getCallButton = view => view.getByTitle('Call');

export const getHangUpButton = view => view.getByTitle('Hang Up');

export const queryHangUpButton = view => view.queryByTitle('Hang Up');
