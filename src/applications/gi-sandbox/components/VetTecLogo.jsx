import React from 'react';

export default function VetTecLogo({ classNames }) {
  return (
    <img
      className={classNames}
      src={'/img/logo/vet-tec-logo.png'}
      alt="Vet Tec Logo"
    />
  );
}
