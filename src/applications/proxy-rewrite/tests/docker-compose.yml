version: '3'
services:
  chrome:
    build: ./chrome
    shm_size: '1gb'
    cap_add:
      - SYS_ADMIN
    ports:
      - '9222'
  vrt:
    build: ./vrt
    volumes:
      - .:/application
    links:
      - chrome