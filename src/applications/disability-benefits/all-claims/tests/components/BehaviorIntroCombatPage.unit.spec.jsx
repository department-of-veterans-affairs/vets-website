import React from 'react';
import { render } from '@testing-library/react';
import { expect } from 'chai';

describe('BehaviorIntroCombatPage', () => {
  describe('Answer Behavioral Questions selection', () => {
    // Radio buttons
    // 1. Selecting Yes and continue advances to Behavior List page
    // 2. Selecting No and continue advances to supporting documents?
  });

  describe('Validations', () => {
    // Radio buttons
    // 1. Can't advance without selecting at least one
    // 2. Displays error message
    //
  });

  describe('Opting out after answering questions already', () => {
    // Displays modal
  });

  describe('Delete question answers on opt out', () => {
    // Deletion stuff
  });
});
