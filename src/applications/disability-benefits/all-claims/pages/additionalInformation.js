import fullSchema from '../config/schema';

const {
  mailingAddress,
  forwardingAddress,
  emailAddress,
  primaryPhone
} = fullSchema526EZ.properties;

export const uiSchema = {

};

export const schema = {
  type: 'object',
  properties: {}
};
