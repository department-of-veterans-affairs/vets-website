import React from 'react';
import { Route } from 'react-router';

import NotificationCenter from './containers/NotificationCenter';

const routes = <Route path="/" component={NotificationCenter} />;

export default routes;
