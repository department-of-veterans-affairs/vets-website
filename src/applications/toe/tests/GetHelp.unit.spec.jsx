import React from 'react';
import { expect } from 'chai';
import GetFormHelp from '../components/GetHelp';

describe('GetFormHelp component', () => {
  it('renders the Get Help footer', () => {
    expect(<GetFormHelp />);
  });
});
