import React from 'react';
import { Route } from 'react-router';
import Authorization from './containers/Authorization';

const routes = <Route path="/" component={Authorization} />;

export default routes;
