const proposed = 'proposed';
const pending = 'pending';
const booked = 'booked';
const arrived = 'arrived';
const fulfilled = 'fulfilled';
const cancelled = 'cancelled';
const noShow = 'no show';
const enteredInError = 'entered in error';
const checkedIn = 'checked in';
const waitList = 'wait listed';

export {
  proposed,
  pending,
  booked,
  arrived,
  fulfilled,
  cancelled,
  noShow,
  enteredInError,
  checkedIn,
  waitList,
};
