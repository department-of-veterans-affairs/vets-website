/**
 * Error types used throughout the AVS application for consistent error handling
 */
export enum ErrorTypes {
  unauthorized = 'unauthorized',
  notFound = 'not_found',
  badRequest = 'bad_request',
}
