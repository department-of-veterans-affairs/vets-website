{
    "name": "vets-website",
    "settings": { 
		"terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
		"dbaeumer.vscode-eslint"
    ],
    "postCreateCommand": "nvm install && yarn install",
    // "postStartCommand": "./.devcontainer/postStartCommand.sh",
}