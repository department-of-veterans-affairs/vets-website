{
    "name": "vets-website",
    "settings": { 
		"terminal.integrated.shell.linux": "/bin/zsh"
    },
    "extensions": [
		"dbaeumer.vscode-eslint"
    ],
    "postCreateCommand": "npm config delete prefix && nvm install && yarn install",
}