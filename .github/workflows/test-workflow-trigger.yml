name: Test Workflow PR Triggers

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  test-pr-trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Log Text
        run: echo ${{ github.event_name }}
