version: '3'
services:
  vets-website:
    build:
      context: .
      args:
        userid: ${UID}
    command: script/review-entrypoint.sh
    environment:
      API_URL: 'foo-api.review.vetsgov-internal'
    expose:
      - 3001
    image: vets-website:${IMAGE_TAG:-latest}
    ports:
      - 3001:3001
    restart: unless-stopped
    volumes:
      - .:/application
      - ../vagov-content:/vagov-content
