{
  "name":"vets-website",
  "env":{
    "INSTALL_HOOKS": "no",
    "CHECK_BROKEN_LINKS": "no",
    "YARN_PRODUCTION":{
      "required": true
    },
    "HEROKU_APP_NAME": {
      "required": true
    },
    "GH_TOKEN": {
      "required": true
    }
  },
  "addons":[],
  "buildpacks": [
    {
      "url": "https://github.com/heroku/heroku-buildpack-nodejs"
    }
  ]
}
